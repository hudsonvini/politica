import styles from "./strip.module.css";

// This component represents a blue strip
export function BlueStrip() {
  // Render a div element with the class name "strip" from the CSS module
  return <div className={styles.strip}></div>;
}
